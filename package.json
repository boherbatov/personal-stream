{
  "name": "personal-streaming-app",
  "version": "0.1.0",
  "private": true,
  "description": "Personal web player for video files forwarded to a Telegram bot.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
